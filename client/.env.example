# Only needed when the API is on a different origin than the app (e.g. `npm run dev` against a remote API).
# In production the Spring Boot jar serves both, so leave this unset.
# VITE_API_URL=https://your-app.awsapprunner.com
